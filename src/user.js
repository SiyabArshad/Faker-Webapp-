import React from 'react'

const User = (props) => {
    //const{data}=props
    return ( <
        >
        <
        h1 > { props.name } < /h1> <
        />
    )
}

export default User